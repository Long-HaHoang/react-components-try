import styled from "styled-components";

const StyledRuler = styled.hr`
  width: 80%;
  height: 2px;
  border-radius: 5px;
  color: var(--secondary-color);
  background-color: var(--secondary-color);
`;

export default StyledRuler;
